// import fly from './fly'

// const host = 'https://10064962.qcloud.la'

// // 这个本来就是一个 请求数据的方法 跟 axios 一样的

// const api = {
//   policy: (param) => fly.get(`${host}/policy`, param),
//   checkTeamUserInfo: (param) => fly.post(`${host}/checkTeamUserInfo`, param)
// }

// export default {
//   install: function(Vue, name='$http') { // 自定义名字
//     Object.defineProperty(Vue.prototype, name, {value: api}) // 将组件库挂载在原型对象上
//   }
// }
